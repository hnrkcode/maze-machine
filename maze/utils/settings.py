# Limit frame rate.
FPS = 60

# Window size.
WIDTH = 1280
HEIGHT = 720
WINDOW_SIZE = (WIDTH, HEIGHT)

# Cells are square with same height and width length.
HIGHEST_CELL_SIZE = 100
LOWEST_CELL_SIZE = 10
CELL_SIZE = HIGHEST_CELL_SIZE

# Size of the maze.
GRID_WIDTH = 900
GRID_HEIGHT = 500
GRID_SIZE = (GRID_WIDTH, GRID_HEIGHT)

# Position of the maze in the window.
GRID_POS = (int(WIDTH / 2 - GRID_WIDTH / 2), int(HEIGHT / 2 - GRID_HEIGHT / 2))

# Colors.
BG_COLOR = (0, 0, 0)
GRID_COLOR = (255, 255, 255)
TEXT_COLOR = (255, 255, 255)

# Text sizes.
LARGE_TEXT = 50
MEDIUM_TEXT = 20
