# Limit frame rate.
FPS = 60

# Window size.
WIDTH = 1280
HEIGHT = 720
WINDOW_SIZE = (WIDTH, HEIGHT)

# Cells are square with same height and width length.
CELL_SIZE = 100

# Sice of the maze.
GRID_SIZE = (900, 500)

# Position of the maze in the window.
GRID_POS = (int(WIDTH / 2 - GRID_SIZE[0] / 2), int(HEIGHT / 2 - GRID_SIZE[1] / 2))

# Colors.
BG_COLOR = (0, 0, 0)
